# Интерфейс «Той таңдау»

Рустем / BIMteam. Обычные HTML, CSS и JavaScript; отдельная сборка не нужна.
Запуск из корня проекта: команда для вашей ОС в [основном README](../README.md).
После установки: `python -m uvicorn app.main:app --no-access-log`, затем открыть `/`.
Контракт сервера: `docs/API.md`. Параметры и границы дат берутся из `/api/options`.
Карточки возвращает `/api/recommend`; интерфейс не ранжирует и не добавляет профили.

Реализованы адаптивная форма, необязательные язык и длительность, примеры запросов,
карточки с подтверждёнными объяснениями и цитатами, отметки синтетических и
восстановленных данных, причины исключения. Различаются отсутствие категории,
отсутствие подходящих по условиям и ошибка сервера. При запросе форма блокируется,
тайм-аут — 15 секунд. После ошибки можно повторить запрос. Ключей в браузере нет.

Основной интерфейс работает с API общей ветки `main`. Проверяемые сценарии:
- ведущие в Алматы, корпоратив, 10.10.2026, 1 000 000 ₸: три карточки;
- флорист в тех же условиях: одна карточка с объяснением;
- бюджет 0: отсутствие совпадений с причинами;
- флорист, Зарубежье: отсутствие категории в городе;
- отключение сервера: сообщение о связи, сохранение условий и повторная попытка;
- узкий экран: форма и карточки располагаются в одну колонку.

Актуальные команды и результаты проверок находятся в [основном README](../README.md).
Старые результаты ветки setup/team-starter не являются проверкой текущей версии.
Реальный NVIDIA API без ключа не проверялся; отображаются объяснения сервера.

Карта и отдельный обзор всех площадок описаны в [VENUE_MAP.md](../docs/VENUE_MAP.md).
В подборе — максимум три карточки с проверкой условий; в обзоре — все площадки
города без проверки даты и бюджета. Все текущие точки обозначены как демонстрационные.

## Сохранение условий и переключение даты

Кнопки «На день раньше» и «На день позже» меняют дату на один календарный день
и запускают новый подбор, сохраняя остальные ограничения. Границы берутся из API;
выход за известный календарь запрещён. Вычисление даты использует UTC.

Условия сохраняются в localStorage только на этом устройстве. После перезагрузки
восстанавливаются доступные значения, но запрос автоматически не отправляется.
Значения старого каталога и некорректные числа игнорируются. При запрете доступа
к хранилищу подбор остаётся доступным. «Сбросить условия» очищает сохранение,
карточки, ошибки и возвращает начальные значения.

Проверено в браузере: переключение на 11.10.2026, восстановление после обновления,
сброс, отключение кнопок на 23.09 и 31.12, отказ отправки отрицательного бюджета,
вёрстка на ширине 390 пикселей. Серверный контракт не менялся.

## Два варианта дизайна

Переключатель над первым экраном позволяет сравнить «01 · Ателье» (тёплый светлый
фон, терракота, заголовки с засечками) и «02 · Сигнал» (графит, лайм, геометрическая
типографика). Форма и результаты при переключении не меняются. Выбор сохраняется
в браузере; при недоступном хранилище переключатель продолжает работать.
Оформление вынесено в `designs.css`, переключение — в `designs.js`.
Оба варианта проверены в браузере на реальных результатах API.
